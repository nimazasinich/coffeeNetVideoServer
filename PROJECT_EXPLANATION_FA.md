# راهنمای کامل SmartCopy Pro

---

## این سیستم چیکار می‌کنه؟

SmartCopy Pro یه سیستم فروش و توزیع رسانه‌ست. برای مکان‌هایی طراحی شده که فیلم و سریال می‌فروشن — مثل فروشگاه‌های کپی فیلم، پرینت‌شاپ، یا کافه‌نت. مشتری از روی صفحه (موبایل یا کامپیوتر) فیلم دلخواهش رو انتخاب می‌کنه، پول می‌ده، و یا فایل روی فلش‌مموریش کپی می‌شه، یا مستقیم دانلود می‌کنه.

---

## چه مشکلی رو حل می‌کنه؟

قبلاً صاحب فروشگاه باید دستی هر مشتری رو مدیریت می‌کرد — پیدا کردن فیلم، کپی کردن، پول گرفتن. این سیستم همه چیز رو خودکار می‌کنه. مشتری خودش انتخاب می‌کنه، سیستم صف درست می‌کنه، پرداخت ثبت می‌شه، و کپی شروع می‌شه.

---

## ۱. مشتری چی می‌بینه و چطور استفاده می‌کنه؟

مشتری یه صفحه ساده می‌بینه:

- **لیست فیلم‌ها و سریال‌ها** با تصویر و اطلاعات
- **جستجو** و **فیلتر** بر اساس دسته‌بندی (فیلم / سریال)
- **انتخاب چند آیتم** برای کپی همزمان
- **انتخاب نوع تحویل**: کپی روی فلش یا دانلود روی موبایل
- **مشاهده قیمت** بر اساس حجم فایل
- **وضعیت لحظه‌ای کپی**: چند درصد انجام شده و چقدر مونده

مشتری می‌تونه پرداخت نقدی یا آنلاین (با کارت بانکی) انجام بده.

---

## ۲. قدم به قدم از نظر مشتری

۱. صفحه باز می‌شه، لیست فیلم‌ها نمایش داده می‌شه
۲. مشتری یه یا چند فیلم رو انتخاب می‌کنه
۳. نوع تحویل رو مشخص می‌کنه (فلش یا دانلود)
۴. قیمت نشون داده می‌شه
۵. مشتری روش پرداخت رو انتخاب می‌کنه:
   - **نقدی**: درخواست ثبت می‌شه، مدیر پول می‌گیره و تأیید می‌کنه
   - **آنلاین**: مشتری به صفحه Stripe می‌ره و کارت می‌کشه
۶. بعد از پرداخت:
   - برای فلش: کپی شروع می‌شه، مشتری پیشرفت رو می‌بینه
   - برای موبایل: یه لینک دانلود یک‌بار مصرف صادر می‌شه

---

## ۳. صف چطور کار می‌کنه؟

هر درخواست یه «کار» (Job) می‌شه. وضعیت‌های ممکن:

- **در انتظار (pending)**: منتظر تأیید پرداخت
- **در صف (queued)**: پرداخت تأیید شده، نوبت نرسیده
- **در حال اجرا (active)**: داره کپی می‌شه
- **انجام شده (completed)**: موفق تموم شد
- **خطا (failed)**: مشکلی پیش اومد
- **لغو شده (cancelled)**: لغو توسط مشتری یا مدیر

سیستم هر ثانیه چک می‌کنه آیا دستگاه آزاده. کارها با اولویت و زمان ثبت مرتب می‌شن. هر دستگاه فلش‌خوان فقط یه کپی همزمان داره.

---

## ۴. کپی روی فلش چطور کار می‌کنه؟

دو نوع دستگاه فلش‌خوان هست:

**محلی**: دستگاه مستقیماً به سرور وصله. سرور فایل رو کپی می‌کنه.

**Agent (از راه دور)**: یه برنامه ویندوزی روی کامپیوتر دیگه‌ای اجرا می‌شه. وقتی فلش وصل می‌شه اطلاع می‌ده. سرور دستور کپی می‌فرسته، Agent دانلود و کپی می‌کنه، پیشرفت رو لحظه‌به‌لحظه گزارش می‌ده.

قبل از شروع کپی، سیستم چک می‌کنه فضای کافی روی فلش هست. اگه نبود، کار با خطای واضح رد می‌شه.

هر Agent باید توسط مدیر تأیید بشه. تأیید نشده = کار دریافت نمی‌کنه.

وقتی یه Agent قطع می‌شه، کارهاش دوباره در صف قرار می‌گیرن.

---

## ۵. دانلود موبایل چطور کار می‌کنه؟

بعد از تأیید پرداخت:

۱. سیستم یه لینک دانلود امن و یک‌بار مصرف می‌سازه
۲. لینک یه زمان انقضا داره (قابل تنظیم)
۳. مشتری با موبایل از همون شبکه Wi-Fi وصل می‌شه و دانلود می‌کنه
۴. اگه اتصال قطع شد، می‌تونه از همون جا ادامه بده (Resume)
۵. لینک بعد از اولین استفاده باطل می‌شه (یک‌بار مصرف)

محدودیت‌های امنیتی:
- هر IP در روز سقف دانلود داره
- تعداد دانلودهای همزمان محدوده
- سرعت دانلود قابل تنظیمه
- لاگ کامل از همه دانلودها ثبت می‌شه

---

## ۶. پرداخت چطور کار می‌کنه؟

**نقدی**: مدیر پول می‌گیره و در پنل تأیید می‌کنه. قیمت بر اساس حجم فایل و جدول نرخ‌ها محاسبه می‌شه.

**آنلاین با Stripe**: سیستم با Stripe ادغام شده. مشتری به صفحه پرداخت Stripe می‌ره. بعد از پرداخت موفق، Stripe به سیستم خبر می‌ده و کار خودکار در صف قرار می‌گیره. برای کار کردن، کلیدهای API در سرور باید تنظیم شده باشن.

پردازش webhook ایدمپوتنت هست، یعنی حتی اگه Stripe یه پیام رو دوبار بفرسته، فقط یه فروش ثبت می‌شه.

---

## ۷. مدیر چی می‌تونه کنترل کنه؟

پنل مدیریت ۷ بخش داره:

**نمای کلی**: CPU، رم، دیسک، آپتایم، کپی‌های امروز، درآمد امروز، تعداد Agent‌های آنلاین

**صف**: لیست کارها، تأیید/رد/لغو، تغییر اولویت، تأیید پرداخت نقدی

**Agent‌ها**: لیست دستگاه‌های متصل با تعداد دقیق کارهای فعال هر کدام، تأیید/رد Agent، تعیین گره اصلی

**کتابخانه رسانه**: اسکن مجدد، قفل/آزادسازی فیلم‌ها

**قیمت‌گذاری**: ویرایش نرخ‌ها بر اساس حجم فایل

**فروش**: تراکنش‌ها و گزارش روزانه تا ۹۰ روز گذشته

**تنظیمات**: تنظیمات سیستم، QR کد، لایسنس، تغییر رمز عبور

---

## ۸. سیستم لایسنس چطور کار می‌کنه؟

سیستم چهار حالت داره:

- **بدون لایسنس (Demo)**: حداکثر ۵ کار همزمان
- **لایسنس پایه (Basic)**: تا ۱۰ کار همزمان
- **لایسنس استاندارد (Standard)**: تا ۵۰ کار همزمان
- **لایسنس حرفه‌ای (Professional)**: تا ۲۰۰ کار همزمان
- **لایسنس سازمانی (Enterprise)**: نامحدود

اگه لایسنس منقضی شده یا دستکاری شده باشه، سیستم کار جدید نمی‌پذیره و یه خطای واضح نشون می‌ده.

مدیر می‌تونه لایسنس رو از طریق پنل (بدون alert/popup مرورگر) وارد کنه.

---

## ۹. اگه سرور ریستارت بشه چی می‌شه؟

هیچ کاری گم نمی‌شه. سیستم هنگام روشن شدن:

۱. کارهایی که وسط کپی موندن رو شناسایی می‌کنه
۲. وضعیتشون رو به «در انتظار» برمی‌گردونه
۳. دوباره در صف قرار می‌گیرن

---

## ۱۰. اگه Agent قطع بشه چی می‌شه؟

کارهایی که به اون Agent واگذار شده بودن، دوباره در صف قرار می‌گیرن. اگه Agent بیش از ۹۰ ثانیه پینگ نفرستاده باشه، سیستم اتصالش رو قطع می‌کنه. هنگام اتصال مجدد، حالت قدیمی پاک می‌شه و Agent از صفر شروع می‌کنه.

---

## ۱۱. امنیت چه تضمین‌هایی داره؟

- رمز ادمین با bcrypt هش می‌شه
- ورود با توکن زماندار
- محدودیت تعداد تلاش برای ورود (حداکثر ۵ بار در ۱۵ دقیقه)
- لینک‌های دانلود یک‌بار مصرف با انقضا
- امضای HMAC برای توکن‌های دانلود
- سقف دانلود روزانه به ازای هر IP
- جلوگیری از دسترسی به فایل‌های خارج از پوشه مجاز
- هدرهای امنیتی HTTP

---

## ۱۲. نقاط قوی سیستم

- صف مدیریت شده با اولویت‌بندی و بازیابی خودکار
- بررسی فضای دیسک قبل از شروع هر کپی
- شمارش دقیق کارهای فعال هر Agent
- لینک‌های دانلود امن با قابلیت Resume
- پردازش ایدمپوتنت پرداخت‌ها (بدون ثبت دوتایی)
- پنل مدیریت کامل بدون alert/prompt مرورگر
- همه خطاها از طریق Toast نمایش داده می‌شن

---

## ۱۳. چه ابزارهایی برای نظارت هست؟

- CPU، رم، دیسک لحظه‌ای
- آپتایم سرور
- تعداد Agent‌های آنلاین با تعداد کار فعال هر کدام
- نمودار مصرف (۱/۷/۳۰ روز گذشته)
- درآمد و کپی‌های امروز
- لاگ کامل تمام رویدادها

---

## ۱۴. چطور راه‌اندازی بشه؟

```
# نصب وابستگی‌های Python
pip install -r requirements.txt

# شروع سرور
uvicorn backend.main:app --host 0.0.0.0 --port 8080

# Build فرانت‌اند
cd frontend_react && npm install && npm run build

# تست‌ها
python3 -m pytest tests/ -v

# Smoke check کامل
bash scripts/smoke_check.sh
```

رمز پیش‌فرض ادمین: `admin` / `admin123!` — بلافاصله تغییر دهید.
